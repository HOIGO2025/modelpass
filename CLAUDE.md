# ModelPass — 铁律

> 本文件是 `施工说明书` 第 1 节的副本。改代码前先读这一节。
> 违反其中任何一条,这个项目就失去了存在的理由。

这个项目的价值不在于"当前有哪些模型"(人人可爬),
而在于"什么时候变了什么"(只能靠时间积累)。
**所有技术决策都服从这一点。**

## 铁律 1:只追加,永不覆盖

数据库里的观测记录**只 INSERT,永不 UPDATE,永不 DELETE**。

同一个模型今天和昨天的记录并存,各自带时间戳。变化通过比对相邻观测得出,不是通过更新字段。

任何 `ON CONFLICT DO UPDATE`、任何 `DELETE FROM observations` 都是 bug。

## 铁律 2:原始响应必须存档

每次 API 返回的原始 JSON 必须原样保存,不做任何加工。

解析逻辑将来一定会改。解析结果可以重算,原始数据丢了就没了。

## 铁律 3:每天一个归档文件,不是每模型一个

小文件会在任何对象存储上产生大量操作费,也让归档难以管理。

**当天全部原始响应打包成一个 `.jsonl.gz`。**

## 铁律 4:采集失败不能静默

任何一天没跑成功,必须留下记录并告警。

时间序列断一天,这一天永远补不回来。宁可报错刷屏,不可静默失败。

## 铁律 5:观测与判断分离

数据库里只存"观测到什么",不存"我认为这意味着什么"。

分析和结论放在单独的表或单独的文件里,永不混入观测记录。

---

## 明确不要做的事

- ❌ 不写 Web 界面(现在没有用户)
- ❌ 不加 ORM、不加 Celery、不加 Docker Compose 多服务
- ❌ 不做去重优化、不做"没变化就跳过"
- ❌ 不下载模型权重
- ❌ 不在采集程序里写任何分析或评分逻辑
- ❌ 不用中文文件名和路径
- ❌ 不把 API key 写进代码

## 自检

```bash
grep -rn "DO UPDATE\|DELETE FROM observations" src/     # 必须无结果
```

`changes` 表是派生数据,可以删了重算;`observations` 不行。
